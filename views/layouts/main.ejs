<!DOCTYPE html>
<html lang="it">
<head>
    <% var SEO = SEO || {}; %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="<%= SEO.sitoOnline || 'index, follow' %>">
    <title><%= SEO.title || 'CMS Spotex SRL' %></title>
    <meta name="description" content="<%= SEO.description || 'CMS Spotex SRL' %>">
    <meta name="keywords" content="<%= SEO.keywords || 'CMS, Spotex, SRL' %>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
</head>
<body>
    <% if (typeof noNavbar === 'undefined') { %>
        <%- include('../partials/navbar') %>
    <% } %>

    <%- body %>
    
    <% if (typeof noFooter === 'undefined') { %>
        <%- include('../partials/footer') %>
    <% } %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>